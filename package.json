{
  "name": "emoji-blaster-multiplayer",
  "version": "2.0.0",
  "description": "Server-authoritative multiplayer mode for Emoji Blaster — Vercel serverless functions + Pusher Channels + Upstash Redis",
  "scripts": {
    "dev": "vercel dev"
  },
  "dependencies": {
    "pusher": "^5.2.0",
    "@upstash/redis": "^1.34.0"
  }
}
